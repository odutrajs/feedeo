# Endereço HTTP do scheduler
HTTP_ADDR=:8090

# Redis
REDIS_ADDR=localhost:6379
REDIS_PASSWORD=
REDIS_DB=0

# Backend Python (FastAPI)
BACKEND_URL=http://localhost:8005

# Canais Redis (não precisa mudar)
SCHEDULE_CHANNEL=scheduler:new
STATUS_CHANNEL=scheduler:status
